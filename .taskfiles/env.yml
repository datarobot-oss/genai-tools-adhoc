version: '3'

tasks:
  update-env:
    desc: "Update environment with dev + all extras (integration tests)"
    cmds:
      - uv sync --all-extras --dev
    silent: true

  update-env-dev:
    desc: "Update environment with dev only"
    cmds:
      - uv sync --dev
    silent: true
